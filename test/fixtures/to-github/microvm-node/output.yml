jobs:
  microvm-node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci --ignore-scripts
        name: Install Dependencies
      - run: npm test
        name: Test
on:
  push: {}
